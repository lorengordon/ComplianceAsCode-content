# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low





- name: Ensure owner on directory /etc/cron.hourly/
  file:
    path: "/etc/cron.hourly/"
    state: directory

    owner: "0"




# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low





- name: Ensure group owner on /etc/cron.daily/
  file:
    path: "/etc/cron.daily/"
    state: directory

    group: "0"




# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low





- name: Ensure group owner on /var/log/
  file:
    path: "/var/log/"
    state: directory

    group: "0"




# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure vim-enhanced is installed
  package:
    name: "vim-enhanced"
    state: present

# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure openssh-clients is installed
  package:
    name: "openssh-clients"
    state: present

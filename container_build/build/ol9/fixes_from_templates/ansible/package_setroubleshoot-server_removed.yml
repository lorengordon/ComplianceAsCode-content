# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low
- name: Ensure setroubleshoot-server is removed
  package:
    name: "setroubleshoot-server"
    state: absent

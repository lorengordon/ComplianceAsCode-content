# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure dracut-fips is installed
  package:
    name: "dracut-fips"
    state: present

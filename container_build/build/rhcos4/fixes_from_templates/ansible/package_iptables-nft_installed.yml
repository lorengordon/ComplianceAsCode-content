# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure iptables-nft is installed
  package:
    name: "iptables-nft"
    state: present

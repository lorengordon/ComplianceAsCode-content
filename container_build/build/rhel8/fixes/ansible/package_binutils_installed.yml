# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure binutils is installed
  package:
    name: binutils
    state: present
  tags:
  - CCE-82989-5
  - enable_strategy
  - low_complexity
  - low_disruption
  - medium_severity
  - no_reboot_needed
  - package_binutils_installed

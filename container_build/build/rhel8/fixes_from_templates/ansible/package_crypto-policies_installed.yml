# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure crypto-policies is installed
  package:
    name: "crypto-policies"
    state: present

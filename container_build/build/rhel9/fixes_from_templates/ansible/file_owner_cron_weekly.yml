# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low





- name: Ensure owner on directory /etc/cron.weekly/
  file:
    path: "/etc/cron.weekly/"
    state: directory

    owner: "0"




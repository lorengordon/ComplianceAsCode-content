# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure libpwquality is installed
  package:
    name: "libpwquality"
    state: present

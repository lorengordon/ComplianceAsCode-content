prodtype: fedora,ol7,ol8,ol9,rhel7,rhel8,rhel9,ubuntu2204
description: 'The system''s default desktop environment, GNOME3, will mount

    devices and removable media (such as DVDs, CDs and USB flash drives) whenever

    they are inserted into the system. To disable autorun-never within GNOME3, add
    or set

    <tt>autorun-never</tt> to <tt>true</tt> in <tt>/etc/dconf/db/local.d/00-security-settings</tt>.

    For example:

    <pre>[org/gnome/desktop/media-handling]

    autorun-never=true</pre>

    Once the settings have been added, add a lock to

    <tt>/etc/dconf/db/local.d/locks/00-security-settings-lock</tt> to prevent user
    modification.

    For example:

    <pre>/org/gnome/desktop/media-handling/autorun-never</pre>

    After the settings have been set, run <tt>dconf update</tt>.'
rationale: 'Automatically mounting file systems permits easy introduction of unknown
    devices, thereby facilitating malicious activity.

    Disabling automatic mount running in GNOME3 can prevent

    the introduction of malware via removable media.

    It will, however, also prevent desktop users from legitimate use

    of removable media.'
severity: low
references:
    cis-csc: 12,16
    cobit5: APO13.01,DSS01.04,DSS05.03,DSS05.04,DSS05.05,DSS05.07,DSS06.03
    cui: 3.1.7
    disa: CCI-000366,CCI-000778,CCI-001958
    isa-62443-2009: 4.3.3.2.2,4.3.3.5.2,4.3.3.6.6,4.3.3.7.2,4.3.3.7.4
    isa-62443-2013: SR 1.1,SR 1.13,SR 1.2,SR 1.4,SR 1.5,SR 1.9,SR 2.1,SR 2.6
    iso27001-2013: A.11.2.6,A.13.1.1,A.13.2.1,A.6.2.1,A.6.2.2,A.7.1.1,A.9.2.1
    nist: CM-7(a),CM-7(b),CM-6(a)
    nist-csf: PR.AC-3,PR.AC-6
    srg: SRG-OS-000114-GPOS-00059,SRG-OS-000378-GPOS-00163,SRG-OS-000480-GPOS-00227
    cis: 1.8.8,1.8.9
identifiers:
    cce: CCE-90257-7
ocil_clause: GNOME autorun is not disabled
ocil: 'These settings can be verified by running the following:

    <pre>$ gsettings get org.gnome.desktop.media-handling autorun-never</pre>

    If properly configured, the output for <tt>autorun-never</tt>should be <tt>true</tt>.

    To ensure that users cannot enable autorun in GNOME3, run the following:

    <pre>$ grep ''autorun-never'' /etc/dconf/db/local.d/locks/*</pre>

    If properly configured, the output for <tt>autorun-never</tt> should be <tt>/org/gnome/desktop/media-handling/autorun-never</tt>'
oval_external_content: null
fixtext: 'Configure GNOME 3 to disable automated mount of removable media.


    The dconf settings can be edited in the /etc/dconf/db/* location.


    First, add or update the [org/gnome/desktop/media-handling] section of the "/etc/dconf/db/local.d/00-security-settings"
    database file and add or update the following lines:


    [org/gnome/desktop/media-handling]

    autorun-never=true


    Then, add the following line to "/etc/dconf/db/local.d/locks/00-security-settings-lock"
    to prevent user modification:


    /org/gnome/desktop/media-handling/autorun-never


    Finally, update the dconf system databases:


    $ sudo dconf update'
checktext: ''
vuldiscussion: ''
srg_requirement: Red Hat Enterprise Linux 9 must disable the graphical user interface
    auto run unless required.
warnings: []
conflicts: []
requires: []
policy_specific_content:
    stig:
        srg_requirement: Red Hat Enterprise Linux 9 must prevent a user from overriding
            the disabling of the graphical user interface autorunner.
        vuldiscussion: Automatically running applications when media is inserted allows
            for the easy introduction of unknown data, thereby facilitating malicious
            activity.
        checktext: 'Verify Red Hat Enterprise Linux 9 disables ability of the user
            to override the graphical user interface autorun setting.


            Determine which profile the system database is using with the following
            command:


            $ sudo grep system-db /etc/dconf/profile/user


            system-db:local


            Check that the automounting setting is locked from non-privileged user
            modification with the following command:


            Note: The example below is using the database "local" for the system,
            so the path is "/etc/dconf/db/local.d". This path must be modified if
            a database other than "local" is being used.


            $ grep ''autorun-never'' /etc/dconf/db/local.d/locks/*


            /org/gnome/desktop/media-handling/autorun-never


            If the command does not return at least the example result, this is a
            finding.'
        fixtext: 'Configure GNOME 3 to not allow a user to change the setting that
            disables automated running of removable media.


            Add the following line to "/etc/dconf/db/local.d/locks/00-security-settings-lock"
            to prevent user modification:


            /org/gnome/desktop/media-handling/autorun-never


            Then update the dconf system databases:


            $ sudo dconf update'
platform: machine
platforms: !!set
    machine: null
sce_metadata: {}
inherited_platforms: !!set
    package[gdm]: null
cpe_platform_names: !!set
    machine: null
inherited_cpe_platform_names: !!set
    package_gdm: null
bash_conditional: null
fixes: {}
title: Disable GNOME3 Automount running
definition_location: /home/oscap/content/linux_os/guide/system/software/gnome/gnome_media_settings/dconf_gnome_disable_autorun/rule.yml
template: null
documentation_complete: true

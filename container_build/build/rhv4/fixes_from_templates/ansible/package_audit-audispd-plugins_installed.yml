# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure audit-audispd-plugins is installed
  package:
    name: "audit-audispd-plugins"
    state: present

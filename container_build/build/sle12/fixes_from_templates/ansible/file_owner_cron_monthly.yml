# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low





- name: Ensure owner on directory /etc/cron.monthly/
  file:
    path: "/etc/cron.monthly/"
    state: directory

    owner: "0"




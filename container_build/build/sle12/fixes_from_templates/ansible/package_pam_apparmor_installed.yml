# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure pam_apparmor is installed
  package:
    name: "pam_apparmor"
    state: present

# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure rsyslog-module-gtls is installed
  package:
    name: "rsyslog-module-gtls"
    state: present

# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low





- name: Ensure owner on directory /var/log/
  file:
    path: "/var/log/"
    state: directory

    owner: "0"




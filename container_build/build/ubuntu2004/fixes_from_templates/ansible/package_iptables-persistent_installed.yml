# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure iptables-persistent is installed
  package:
    name: "iptables-persistent"
    state: present

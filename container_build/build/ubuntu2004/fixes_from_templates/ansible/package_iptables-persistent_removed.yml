# platform = multi_platform_all
# reboot = false
# strategy = disable
# complexity = low
# disruption = low
- name: Ensure iptables-persistent is removed
  package:
    name: "iptables-persistent"
    state: absent

# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure opensc-pkcs11 is installed
  package:
    name: "opensc-pkcs11"
    state: present

# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure libpam-pwquality is installed
  package:
    name: "libpam-pwquality"
    state: present

# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure systemd-timesyncd is installed
  package:
    name: "systemd-timesyncd"
    state: present

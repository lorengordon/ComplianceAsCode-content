

<def-group>
  <definition class="compliance" id="sshd_use_directory_configuration" version="1">
   <metadata>
        <title>Distribute the SSH Server configuration to multiple files in a config directory.</title>
        
    <affected family="unix">
    <platform>Red Hat Enterprise Linux 9</platform>
      <platform>Oracle Linux 9</platform>
      <platform>Fedora</platform>
    </affected>
        <description>foo</description>
    </metadata>
   <criteria comment="sshd is configured correctly or is not installed" operator="OR">
     <criteria comment="sshd is not installed" operator="AND">
        <extend_definition comment="sshd is not required or requirement is unset"
          definition_ref="sshd_not_required_or_unset" />
        <extend_definition comment="rpm package openssh-server removed"
          definition_ref="package_openssh-server_removed" />
     </criteria>
     <criteria comment="sshd is installed and configured" operator="AND">
        <extend_definition comment="sshd is required or requirement is unset"
          definition_ref="sshd_required_or_unset" />
        <extend_definition comment="rpm package openssh-server installed"
          definition_ref="package_openssh-server_installed" />
        <criteria comment="sshd is configured correctly" operator="AND">
          <extend_definition comment="sshd includes config files from its .d directory"
            definition_ref="sshd_includes_config_files" /><criterion comment="Check the absence of match in /etc/ssh/sshd_config"
          test_ref="test_sshd_use_directory_configuration_default_not_overriden" />
        </criteria>
      </criteria>
    </criteria> 
  </definition>
  
  <ind:textfilecontent54_test check="all" check_existence="none_exist"
    comment="tests the absence of match setting in the /etc/ssh/sshd_config file"
    id="test_sshd_use_directory_configuration_default_not_overriden" version="1">
    <ind:object object_ref="obj_sshd_use_directory_configuration_default_not_overriden" />
  </ind:textfilecontent54_test>
  
  <ind:textfilecontent54_object id="obj_sshd_use_directory_configuration_default_not_overriden" version="1">
    <ind:filepath>/etc/ssh/sshd_config</ind:filepath>
    <ind:pattern operation="pattern match">^[ \t]*(?i)match(?-i)\s+\S+</ind:pattern>
    <ind:instance operation="greater than or equal" datatype="int">1</ind:instance>
  </ind:textfilecontent54_object>
</def-group>

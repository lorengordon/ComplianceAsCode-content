# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low





- name: Ensure group owner on /etc/cron.d/
  file:
    path: "/etc/cron.d/"
    state: directory

    group: "0"




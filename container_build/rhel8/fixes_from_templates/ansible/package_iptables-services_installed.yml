# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure iptables-services is installed
  package:
    name: "iptables-services"
    state: present

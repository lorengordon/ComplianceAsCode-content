# platform = multi_platform_all
# reboot = false
# strategy = enable
# complexity = low
# disruption = low
- name: Ensure libpam-pkcs11 is installed
  package:
    name: "libpam-pkcs11"
    state: present

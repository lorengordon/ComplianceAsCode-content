# platform = multi_platform_all
# reboot = false
# strategy = configure
# complexity = low
# disruption = low





- name: Ensure group owner on /etc/cron.weekly/
  file:
    path: "/etc/cron.weekly/"
    state: directory

    group: "0"



